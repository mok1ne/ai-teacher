# --- ИИ (обязательно, ключ только на сервере) ---
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx

# --- подпись сессионных токенов (ОБЯЗАТЕЛЬНО в проде!) ---
# Сгенерируйте: openssl rand -base64 48  (или: node -e "console.log(require('crypto').randomBytes(48).toString('base64'))")
# Без сильного секрета в проде вход отключается (токены нельзя подделать).
AUTH_SECRET=Cqa8arrvRpIaiZiG3l7ZPXjSKyrrb1uYny4wOxkrs4JB/MggGspBcaCTTz1kgMXl

# --- VK ID (https://id.vk.com / vk.com/apps -> ваше приложение) ---
VK_APP_ID=
VK_SECURE_KEY=
# тот же ID приложения VK — нужен фронту для редиректа на авторизацию:
VITE_VK_APP_ID=

# --- ЮKassa (https://yookassa.ru) ---
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=

# (необязательно) переопределить адрес бэкенда ИИ. По умолчанию /api/chat
# VITE_API_URL=/api/chat

# --- База данных (Neon / Postgres) ---
DATABASE_URL=

# --- Отправка писем (восстановление пароля). Без ключа код показывается в демо. ---
RESEND_API_KEY=
MAIL_FROM=Время сдавать <onboarding@resend.dev>
